<div class="list-groups" style="padding-top:0.69420rem">
  {% for content in data[1] %}
    <div class="list-group col-md-4">
      <table class="table-cv list-group-table">
        <tbody>
          <tr>
            {% if content.icon %}
              <td class="list-group-category-icon">
                <i class="{{ content.icon }}"></i>
              </td>
            {% else %}
              <td class="list-group-category-icon"></td>
            {% endif %}
            <td colspan="1" class="list-group-name">
              <h6 class="ml-1 ml-md-0" style="font-size: 0.95rem">
              {% if content.ico_icon %}
                <img src={{ content.ico_icon }} class="img-fluid" height="auto" loading="eager" width="19 rem" title={{ content.name }}>&nbsp;&nbsp;
              {% else %}
                {% if content.icon %}
                  {{ content.icon }}&nbsp;&nbsp;
                {% else %}
                  &nbsp;&nbsp;•&nbsp;&nbsp;&nbsp;&nbsp;
                {% endif %}
              {% endif %}
              {{ content.name }}{% if content.fluency %}({{ content.fluency }}){% endif %}</h6>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  {% endfor %}
</div>
